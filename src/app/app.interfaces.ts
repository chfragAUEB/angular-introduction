export interface User {
  firstName: string;
  lastName: string;
  age: number;
}

export interface SideBar {
  text: string;
  path: string;
}

export interface Joke {
  value: string;
}
